<Window x:Class="WindowsTemplate.GlowWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controlzEx="clr-namespace:ControlzEx.Controls"
        xmlns:local="clr-namespace:WindowsTemplate"
        mc:Ignorable="d"
        Title="GlowWindow" Height="450" Width="800">
    <Window.Resources>
        <!--  use this storyboard for a smoother visibility  -->
        <Storyboard x:Key="ControlzEx.GlowWindow.OpacityStoryboard">
            <DoubleAnimation BeginTime="00:00:.2"
                             Storyboard.TargetName="glowWindow"
                             Storyboard.TargetProperty="(UIElement.Opacity)"
                             To="1"
                             Duration="00:00:.3" />
        </Storyboard>
    </Window.Resources>

    <Window.Template>
        <ControlTemplate TargetType="Window">
            <ContentPresenter />
        </ControlTemplate>
    </Window.Template>

    <local:Glow x:Name="glow"  
                     x:FieldModifier="private" />
</Window>
